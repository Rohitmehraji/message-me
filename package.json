{
  "name": "message-me",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run start",
    "build": "npm run -ws build",
    "lint": "npm run -ws lint",
    "reset:deps": "node scripts/reset-deps.mjs"
  }
}
